1、若远程仓库他人新添加分支了，如何更新至本地仓库和工作区？--远程库修改1--远程库修改2--本地库修改2远程修改3
	a、git fetch origin//从远程仓库 拉取至 本地仓库
	b、git pull origin//从远程仓库 拉取至 工作区


2、若远程仓库版本被回退了，如何更新至本地？
	a、git reset --hard origin//来保持与远程库一致
	b、git pull origin//拉最新版本至本地库

3、若远程库版本被回退了，并且有提交新的版本，如何更新本地？
	a、git pull origin//拉最新版本至本地库 并且会与本地merge

？？？？	
4、回退一个版本,不清空暂存区,将已提交的内容恢复到暂存区,不影响原来本地的文件(未提交的也不受影响)
	a、git reset --soft 版本id



5、添加修改至工作区？
	a、git add 文件名//文件名改成‘.’则添加所有
	
6、提交修改至工作区？
	a、git commit -m "日志信息"
	
7、再次提交：上次提交有遗漏？
	a、git commit --amend


场景练习1补充
8、添加至工作区并提交？
	锕、git commit -a -m "日志信息"



1、初始化
	a、git init//对当前文件夹进行git初始化，生成.git配置目录
	b、git remote -v //显示远程仓库的基本信息 
	c、git remote show origin //显示远程仓库的详细信息，包括哪些分支
			
2、分支管理
	更新分支：
		a、git fetch origin
		b、git pull origin
		场景一、如果远程库上有某人新添加了分支？
			1、使用命令a，访问远程仓库，从中拉取所有你还没有的数据
			2、再用命令b
	查看分支：git branch
	创建分支：git checkout -b 分支名
	切换分支：git checkout 分支名
	合并分支：git merge 分支名
	合并冲突查询：git mergetool
	分支差异：git log --left-right 分支1...分支2 
	删除分支：git branch -d 分支名
	
3、历史记录
	查看所有：git log
		继续向前查看：回车键 退出：q
	git log --oneline
		查看记录每行显示
		
4、版本回退
	a、git reset --hard origin
	b、git pull origin
	c、git reset --soft 版本id
	场景一：如果远程库版本被回退了，如何更新本地？
			1、使用命令a，来保持与远程库一致
			2、再用命令b，拉最新版本至本地库
	场景二、如果远程库版本被回退了，并且有提交新的版本，如何更新本地？
			1、使用命令b，拉最新版本至本地库、并且会与本地merge
	场景三、回退一个版本,不清空暂存区,将已提交的内容恢复到暂存区,不影响原来本地的文件(未提交的也不受影响)
			使用命令c
	
5、管理修改
	a、git add xxx
	b、git commit -m "yyy"
	c、git commit --amend
	d、git commit -a -m "yyy"
	场景一、添加修改至工作区？
			1、使用命令a
	场景二、未发生提交，提交修改至工作区？
			1、使用命令b
	场景三、上一次commit后发现有遗漏，需要再次commit？
			1、使用命令c，此命令就会撤销上次的commit，提交本次
	场景四、快捷提交？
			1、使用命令d，添加修改至工作区且提交
	
6、撤销修改
	a、git checkout -- 文件名
	   git checkout .
	b、git reset HEAD 文件名
	
	场景一、当你改乱了工作区某个文件的内容、想直接丢弃工作区的修改时？
			1、使用命令a
	场景二、当你不但改乱了工作区某个文件的内容、还添加到了暂存区时、想丢弃修改？
			1、使用命令b
			2、使用命令a
			
7、删除文件
	a、rm 文件名
	b、git rm 文件名
	场景一、删除本地文件
			1、使用命令a
	场景二、删除远程库文件
			1、使用命令b

8、存储本地仓库
	git stash
	
9、查看某人提交记录
	git log --author=“某人”
	

hf：处理一个紧急bug
dev:开内容


	
参考网址：https://gitee.com/liaoxuefeng/learn-java/raw/master/teach/git-cheatsheet.pdf
